<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context="hva.pages.OpenApps"
    android:orientation="vertical"
    >
    <TextView
        style="@style/AvtivityTitleStyle"
        android:text="\n \nOpen Apps"
        />

    <ListView
        android:id="@+id/AppsList_ListView_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        </ListView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >
        <TextView
            android:id="@+id/Title_textview_id"
            android:text="@string/Title_textview_text"
            android:visibility="invisible"
            style="@style/Title"
            />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:fillViewport="true">
            <TextView
                android:id="@+id/Code_textview_id"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/CodeFont"
                android:layout_weight="1.0"/>
        </ScrollView>
    </LinearLayout>
</LinearLayout>
